export var OPTIONS ={
   customAuth : 'string',
   discover : {
       commonWords : '',
   },
   test : {
       vectors : 'string',
       sensitive: 'string',
       random: true,
       slow : 500
   }
 
}